import { CustomFechaPagoPipe } from './custom-fecha-pago.pipe';

describe('CustomFechaPagoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFechaPagoPipe();
    expect(pipe).toBeTruthy();
  });
});
