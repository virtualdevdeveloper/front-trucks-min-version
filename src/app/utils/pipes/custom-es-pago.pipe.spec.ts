import { CustomEsPagoPipe } from './custom-es-pago.pipe';

describe('CustomEsPagoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomEsPagoPipe();
    expect(pipe).toBeTruthy();
  });
});
